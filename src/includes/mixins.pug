//- includes/mixins.pug

mixin lesson(name, link, nextLink)
	h1= name
	.video
		.youtube(data-embed=link)
			.play-button
	if nextLink
		.next
			a(href=nextLink) Pokračovat na další lekci

mixin button(name, link)
	.button
		a(href=link)=name
